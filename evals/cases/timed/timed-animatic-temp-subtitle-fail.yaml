id: timed-animatic-temp-subtitle-fail
title: Composition-side subtitle re-split must fail
suite: timed-animatic
case_kind: negative-baseline
expected_result: fail
slice:
  input_mode: locked-script
  stage: timed-animatic

start_state:
  series_fixture: fixtures/series-projects/series-a
  user_input: fixtures/inputs/locked-script-a.md
  injected_failure: fixtures/expected-boundaries/timed-animatic-temp-subtitle.md

target_stage: timed-animatic

must_do:
  - drive all displayed subtitles from captions.json only

must_not_do:
  - re-split subtitles inside Composition by punctuation or char count

gates:
  - captions_sole_timeline

notes: |
  Negative baseline: temporary composition subtitle splitting.
