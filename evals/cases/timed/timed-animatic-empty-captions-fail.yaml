id: timed-animatic-empty-captions-fail
title: Empty captions.json during Timed Animatic must fail
suite: timed-animatic
case_kind: negative-baseline
expected_result: fail
slice:
  input_mode: locked-script
  stage: timed-animatic

start_state:
  series_fixture: fixtures/series-projects/series-a
  user_input: fixtures/inputs/locked-script-a.md
  injected_failure: fixtures/expected-boundaries/timed-animatic-empty-captions.md

target_stage: timed-animatic

must_do:
  - write formal captions.json timeline from real audio

must_not_do:
  - leave captions.json empty after formal TTS path

gates:
  - formal_captions
  - full_playback

notes: |
  Negative baseline: TTS path without formal captions timeline.
