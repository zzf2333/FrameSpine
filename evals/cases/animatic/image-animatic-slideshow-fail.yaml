id: image-animatic-slideshow-fail
title: Default camera on all images or pure slideshow must fail
suite: image-animatic
case_kind: negative-baseline
expected_result: fail
slice:
  input_mode: locked-script
  stage: image-animatic

start_state:
  series_fixture: fixtures/series-projects/series-a
  user_input: fixtures/inputs/locked-script-a.md
  injected_failure: fixtures/expected-boundaries/image-animatic-slideshow.md

target_stage: image-animatic

must_do:
  - provide rough but readable Entry Development Emphasis Handoff
  - use series Motion Affordances / Motion Language

must_not_do:
  - apply the same default camera motion to every image
  - deliver a pure slideshow with no temporal directing

gates:
  - motion_structure
  - full_playback

notes: |
  Negative baseline: mechanical slideshow / default push-in on all cards.
