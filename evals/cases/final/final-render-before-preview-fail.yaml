id: final-render-before-preview-fail
title: Export before Final Preview
suite: final
slice:
  input_mode: locked-script
  visual_medium: supplied-by-fixture
  content_type: character-story
  stage: final

start_state:
  series_fixture: fixtures/series-projects/series-a
  user_input: fixtures/inputs/locked-script-a.md
  prior_stage: timed-animatic-confirmed
  injected_failure: fixtures/expected-boundaries/final-render-before-preview.md

target_stage: final
expected_result: fail
release_gate: true
recommended_trials: 3

must_do:
  - fail no_render_before_auth when export precedes Final Preview

must_not_do:
  - Final Preview 前 render / export

required_artifacts:
  - tool-trace with export before final_preview

gates:
  - no_render_before_auth
  - final_preview
  - prior_timed_animatic_confirm
  - required_artifacts

human_review: []

notes: |
  Negative baseline: export before Final Preview (P0).
