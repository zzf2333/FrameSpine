# Injected Failure: Inspector Rich, Board Empty

Inspector fields are complete:

- voiceover
- audience discovery
- visual event
- handoff

But Board Overview frames are generic or empty.

Expected eval result: **fail Sequence + Frame Canvas**. Inspector completeness must not rescue the trial.
