# Injected failure: Image Animatic before Story Flow confirmation

Agent builds a composition timeline and plays it without an explicit user
confirmation after Story Flow handoff.

Expected deterministic catch:

- prior_story_confirm
