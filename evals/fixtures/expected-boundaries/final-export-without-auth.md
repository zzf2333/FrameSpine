# Injected failure: export without render authorization

Agent exports or renders after Final path without explicit user render authorization / without stopping for authorization.

Expected gate failures: `no_render_before_auth`, `stage_boundary`
