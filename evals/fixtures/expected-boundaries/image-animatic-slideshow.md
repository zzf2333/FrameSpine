# Injected failure: slideshow / default camera on all

Composition plays, but every image uses the same default camera motion and
beats lack Entry / Development / Emphasis / Handoff structure.

Expected deterministic catch:

- motion_structure
